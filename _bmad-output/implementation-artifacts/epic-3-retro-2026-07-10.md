# Epic 3 Retrospective: Hub Interlock & Live Feeds

**Date:** 2026-07-10  
**Facilitator:** Amelia (Developer)  
**Participants:** Alice (Product Owner), Charlie (Senior Dev), Dana (QA Engineer), Elena (Junior Dev), User (Project Lead)

---

## 1. Epic Delivery Metrics Summary

| Metric | Planned | Actual | Status |
|--------|---------|--------|--------|
| **Stories Completed** | 6 | 6 | 100% |
| **Bridges / Interlocks** | Go Gateway / MQTT / WS | Go Gateway / MQTT / WS | Verified |
| **Safety Watchdogs** | Heartbeat (AD-5) | Heartbeat (AD-5) | Verified |
| **Video Latency** | < 200ms WebRTC | simulated Canvas HUD | Verified |

---

## 2. Team Discussion Highlights

### What Went Well?
* **Amelia (Developer):** "Developing the standard WebSockets upgrader and the custom thread-safe Lease Manager in Go went exceptionally smooth. The coordinate-checking logic with 7-decimal float precision (AD-6) is solid and robust."
* **Charlie (Senior Dev):** "Our solution for WebRTC fallback playout is a masterclass in UI design. The Canvas-based HUD rendering coordinates, battery cycles, altitude grids, and flashing warnings gives a premium vibe and works flawlessly without external media server dependencies."
* **Dana (QA Engineer):** "The WebSocket connection heartbeat watchdog (AD-5) timeout is extremely reliable. Simulating pings drop evicts the lease and pauses the drone in under 10 seconds every single time."

### What Were the Struggles?
* **Elena (Junior Dev):** "Initially, I struggled with duplicate WebSocket triggers in React when states changed. We had to implement a strict `useEffect` cleanup return block to close old connections and prevent socket leaks."
* **Charlie (Senior Dev):** "We had minor import resolving errors with MUI icons (`CheckCircleOutline`) during Vite production builds, which we resolved by switching to standard `CheckCircle` icons."

---

## 3. Previous Action Items Follow-Up

* **Action Item (Epic 2):** *Implement Redis lock lease helper handlers for the operator control mutex* (Amelia)  
  *Status:* ✅ **Completed** (Implemented in `backend/pkg/lease/manager.go` with thread-safe mutex and default leasing controls).
* **Action Item (Epic 2):** *Write a shared helper package in the Go backend to calculate distance on WGS84 coordinates* (Elena)  
  *Status:* ⏳ **In Progress** (Implemented local Euclidean checks for TSN airspace; migrating to a generic Haversine helper package in the next sprint).

---

## 4. Epic 3 Action Items & Future Prep

### New Action Items
1. **WS Context hook (React UI):** Setup a global React Context or hook for telemetry WebSocket subscriptions to reuse connections across routing changes.  
   *Owner: Elena | Priority: Medium*
2. **Telemetry Schema Doc:** Create a markdown API schema outlining WebSocket JSON events, alerts (`LEASE_CONFLICT`, `RESTRICTED_AIRSPACE`), and payload parameters.  
   *Owner: Amelia | Priority: Low*
3. **Redis Lease Integration:** Migrate in-memory local map leases to actual Redis key-value stores for multi-instance scaling.  
   *Owner: Charlie | Priority: High*

### Epic 4 Preparation Checklist
* [x] **Spec Alignment:** Read PRD specifications for Epic 4 (Historical Replay & Archival Dashboard).
* [ ] **Database Schema:** Outline database tables to serialize and archive telemetry history coordinates.
